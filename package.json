{
  "name": "chesshub",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "npm run build && node dist/server.js",
    "setup": "npm run build && node dist/setup-host-key.js && ./setup-repo.sh"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ssh2": "^1.15.6",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "ssh2": "^1.17.0"
  }
}
