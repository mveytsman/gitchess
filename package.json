{
  "name": "chesshub",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test test/*.test.mjs",
    "start": "npm run build && node dist/server.js",
    "setup": "npm run build && bash scripts/setup-host-key.sh && bash scripts/setup-repo.sh"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/ssh2": "^1.15.6",
    "typescript": "^5.9.0"
  },
  "dependencies": {
    "ssh2": "^1.17.0"
  }
}
